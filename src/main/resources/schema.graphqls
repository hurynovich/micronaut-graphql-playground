type Query {
    bookById(id: ID): Book
}

type Book {
    id: ID
    name: String
    pageCount: Int
    author: Author
    price: Float
}

type Author {
    id: ID
    firstName (min: Int = 3, max: Int = 64) : String
    lastName: String
}
